---
import { getContent } from '../content';
import type { Locale } from '../content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectStudio from './ProjectStudio.astro';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const copy = getContent(locale);
---

<BaseLayout
  locale={locale}
  route="work"
  title={copy.pageMeta.work.title}
  description={copy.pageMeta.work.description}
>
  <ProjectStudio
    locale={locale}
    projects={copy.projects}
    sections={copy.sections}
    visuals={copy.visuals}
    actions={copy.actions}
    mode="full"
    workIntro={copy.workIntro}
    headingTag="h1"
  />
</BaseLayout>
